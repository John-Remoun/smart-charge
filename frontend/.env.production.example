# Copy to .env.production locally, or set in Vercel / Netlify project settings.
# Use your deployed API base (scheme + host + port if any), NO trailing slash.
REACT_APP_API_ORIGIN=https://your-backend.example.com
